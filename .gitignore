.claude/

# ── กันไฟล์ข้อมูลจริง/ไฟล์งานหลุดขึ้น GitHub Pages (สาธารณะ) ──
# ข้อมูลผลผลิต/ไก่ตายจริง ห้ามขึ้นเว็บ
*.xlsx
*.xls
*.csv
# รูปแชต/สกรีนช็อตงาน (ยกเว้นภาพหน้าปกแอปที่ตั้งใจ deploy)
*.jpg
*.jpeg
*.png
!home-render.jpg
# สคริปต์ฝั่งเครื่อง/แบ็คอัพ
*.ps1
*.bat
# แดชบอร์ดเฉพาะกิจ + ไฟล์ mockup/ทดลอง + ไฟล์สำรอง
dashboard_*.html
_*.html
*_backup*.json
PPF_backup_*.json
# ข้อมูลฟาร์มที่ export/กู้คืน (JSON) — ห้ามขึ้น public
PPF_*.json
*_กู้คืน.json
# แอปเวอร์ชันไฟล์เดียวรุ่นเก่า
เครื่องคำนวณ*.html
# เอกสารภายใน (มีโครงสร้างระบบ/PIN — ไม่ควรขึ้น public)
*_README.md
สรุป*.md
